<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Admin Login - MineNepal</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.3/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', 'Segoe UI', sans-serif;
            background: #f5f7fa;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .login-container {
            width: 100%;
            max-width: 420px;
        }

        .login-card {
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 24px rgba(0, 0, 0, 0.06);
            padding: 48px 40px;
            border: 1px solid #e8ecef;
        }

        .logo-section {
            text-align: center;
            margin-bottom: 40px;
        }

        .logo-icon {
            width: 64px;
            height: 64px;
            background: #0d6efd;
            border-radius: 16px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 16px;
        }

        .logo-icon i {
            font-size: 32px;
            color: white;
        }

        .logo-title {
            font-size: 24px;
            font-weight: 700;
            color: #1a1d1f;
            margin-bottom: 8px;
        }

        .logo-subtitle {
            font-size: 14px;
            color: #6c757d;
            font-weight: 400;
        }

        .form-label {
            font-size: 14px;
            font-weight: 600;
            color: #1a1d1f;
            margin-bottom: 8px;
        }

        .form-control {
            height: 48px;
            border: 1.5px solid #e8ecef;
            border-radius: 10px;
            padding: 12px 16px;
            font-size: 15px;
            transition: all 0.2s ease;
            background: #fafbfc;
        }

        .form-control:focus {
            background: white;
            border-color: #0d6efd;
            box-shadow: 0 0 0 4px rgba(13, 110, 253, 0.08);
            outline: none;
        }

        .form-control::placeholder {
            color: #9ca3af;
        }

        .input-group {
            position: relative;
        }

        .input-icon {
            position: absolute;
            left: 16px;
            top: 50%;
            transform: translateY(-50%);
            color: #6c757d;
            font-size: 16px;
            z-index: 10;
        }

        .input-group .form-control {
            padding-left: 44px;
        }

        .btn-login {
            width: 100%;
            height: 48px;
            background: #0d6efd;
            border: none;
            border-radius: 10px;
            color: white;
            font-size: 15px;
            font-weight: 600;
            transition: all 0.2s ease;
            margin-top: 24px;
        }

        .btn-login:hover {
            background: #0b5ed7;
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(13, 110, 253, 0.3);
        }

        .btn-login:active {
            transform: translateY(0);
        }

        .alert {
            border-radius: 10px;
            border: none;
            padding: 12px 16px;
            font-size: 14px;
            margin-bottom: 24px;
        }

        .alert-danger {
            background: #fee;
            color: #c41e3a;
        }

        .divider {
            text-align: center;
            margin: 32px 0;
            position: relative;
        }

        .divider::before {
            content: '';
            position: absolute;
            left: 0;
            top: 50%;
            width: 100%;
            height: 1px;
            background: #e8ecef;
        }

        .divider span {
            background: white;
            padding: 0 16px;
            position: relative;
            color: #6c757d;
            font-size: 13px;
            font-weight: 500;
        }

        .info-text {
            text-align: center;
            color: #6c757d;
            font-size: 13px;
            margin-top: 24px;
        }

        .info-text i {
            color: #0d6efd;
            margin-right: 4px;
        }

        @media (max-width: 576px) {
            .login-card {
                padding: 32px 24px;
            }

            .logo-title {
                font-size: 20px;
            }

            .form-control,
            .btn-login {
                height: 44px;
            }
        }
    </style>
</head>

<body>
    <div class="login-container">
        <div class="login-card">
            <div class="logo-section">
                <div class="logo-icon">
                    <i class="fas fa-gamepad"></i>
                </div>
                <h1 class="logo-title">MineNepal</h1>
                <p class="logo-subtitle">Admin Dashboard</p>
            </div>

            <% if (error && error.length> 0) { %>
                <div class="alert alert-danger">
                    <i class="fas fa-exclamation-circle me-2"></i>
                    <%= error[0] %>
                </div>
                <% } %>

                    <div id="location-error" class="alert alert-danger" style="display: none;"></div>

                    <form id="login-form" action="/auth/login" method="POST">
                        <div class="mb-3">
                            <label for="username" class="form-label">Username</label>
                            <div class="input-group">
                                <i class="fas fa-user input-icon"></i>
                                <input type="text" class="form-control" id="username" name="username"
                                    placeholder="Enter your username" required autofocus>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label for="pass1" class="form-label">Password 1</label>
                            <div class="input-group">
                                <i class="fas fa-lock input-icon"></i>
                                <input type="password" class="form-control" id="pass1" name="pass1"
                                    placeholder="First password" required>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label for="pass2" class="form-label">Password 2</label>
                            <div class="input-group">
                                <i class="fas fa-key input-icon"></i>
                                <input type="password" class="form-control" id="pass2" name="pass2"
                                    placeholder="Second password" required>
                            </div>
                        </div>

                        <!-- Cloudflare Turnstile Widget -->
                        <div class="mb-3 d-flex justify-content-center">
                            <div class="cf-turnstile" data-sitekey="0x4AAAAAAB-b-PLsOvYxdt0U"></div>
                        </div>

                        <button type="submit" class="btn btn-login">
                            <i class="fas fa-sign-in-alt me-2"></i>Login
                        </button>
                    </form>

                    <div class="divider">
                        <span>Secure Access</span>
                    </div>

                    <p class="info-text">
                        <i class="fas fa-shield-alt"></i>
                        Protected by authentication
                    </p>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.3/js/bootstrap.bundle.min.js"></script>
</body>

</html>